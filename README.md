# Rate_limiting
This project shows you the use of different rate limiting algorithm using a queue and set value given by the user

Create a virtual enviorment 
install the libraries in requirement.txt 
and run uvicorn app:main --reload
